# 需求：
帮我完成三个测试代码，分别是，
test\test1\test1.ino
test\test2\test2.ino
test\test3\test3.ino

# 需求说明
1. 测试1程序：我需要测试同时测试isConnected、getHeartbeat这两个APi,帮我写个测试程序

2. 测试2程序：我需要测试 setInstallInfo(...)、getInstallInfo(...)这两个API,帮我实现写入安装程序，读出安装信息，打印出安装信息，确保安装信息正常使用

3. 测试3程序：我需要，单独测试这一个API setTag,给合适的参数写入，逻辑是先清除所有标签,连续确认5次只有一个轨迹目标，然后使用set_tag设置一个标签，再将标签配置信息读出打印出来

3. 测试4程序：我需要单独测试一个API clearTag，清除test3.ino中指定的索引的标签，删除前读出一下，删除后再读出一下

