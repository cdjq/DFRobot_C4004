# DFRobot_C4004 修改记录
原则：将修改添加进记录，记录在修改记录.md（注意是添加，不要修改我原本的内容）

## 2026-07-29 — 编码规范整改（C++ 库）

### 1. 邮箱统一
- 全局将作者邮箱 `zhixin.liu@dfrobot.com` 替换为 `jia.li@dfrobot.com`

### 2. enum class 改为 typedef enum
- `eBoundaryDirection_t`、`eApproachAwayDirection_t` 由 `enum class` 改为 `typedef enum`，与库内其它枚举风格一致
- 因两枚举原均含 `eNone`，迁入 class 后会冲突，拆分为：
  - `eBoundaryNone`
  - `eApproachAwayNone`

### 3. 全局宏统一 `C4004_` 前缀
- 原先部分宏已有 `C4004_` 前缀（如 `C4004_MAX_POINTS`），其余不统一
- 现将协议/超时等相关宏统一加前缀，例如：
  - `QUERY_DATA` → `C4004_QUERY_DATA`
  - `FRAME_HEAD1` → `C4004_FRAME_HEAD1`
  - `DEFAULT_TIMEOUT` → `C4004_DEFAULT_TIMEOUT`
  - `CTRL_*` → `C4004_CTRL_*`
  - `CMD_*` → `C4004_CMD_*`
- 未改：头文件保护宏 `DFROBOT_C4004_H`、调试宏 `DBG` / `ENABLE_DBG`
- 去掉类内多余的 `static const MAX_*` 别名，对外尺寸常量直接使用 `C4004_MAX_*` 宏

## 2026-07-29 — 枚举 / 结构体定义调整

### 1. 删除未使用的 Unknown 枚举值
- `ePresenceState_t`：删除 `ePresenceUnknown = 0xFF`，默认存在状态改为 `eNoPresence`
- `eMotionState_t`：删除 `eMotionUnknown = 0xFF`，默认运动状态改为 `eMotionNone`
- `eTargetFeature_t`：删除 `eFeatureUnknown = 0xFF`

### 2. 精简 `eDetectionRangeMode_t`
- 删除库内未使用的模式：`eRangeSideDefault`、`eRangeSideLeftEdge`、`eRangeSideRightEdge`、`eRangeHotelCorridor`、`eRangeNoBoundary`、`eRangeTopDefault`、`eRangeTopLeftEdge`、`eRangeTopRightEdge`
- 保留：`eRangeFourSide`、`eRangeTrajectory`、`eRangeConfigFile`、`eRangeUnknown`
- Python 侧对应常量同步删除

### 3. 调整 `sFourSidedRange_t`
- 删除冗余字段 `mode`（设置四面边界时协议固定为 `eRangeFourSide`，调用方传入无效）
- 字段重命名：
  - `xPositiveCm` → `xMax`
  - `xNegativeCm` → `xMin`
  - `yPositiveCm` → `yMax`
  - `yNegativeCm` → `yMin`
- 检测范围模式改为类内独立缓存 `_rangeMode`（Python：`_range_mode`）
- 已同步 Arduino / Python 示例与 README 中的字段名

## 2026-07-29 — API 重命名

### 1. 上报事件获取接口
- `getReportedInfo()` → `getReportedEvent()`
- Python：`get_reported_info()` → `get_reported_event()`
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

### 2. 安装高度接口
- `setInstallHigh()` → `setInstallHeight()`
- `getInstallHigh()` → `getInstallHeight()`，参数 `pHight` → `pHeight`
- Python：`set_install_high()` → `set_install_height()`，`get_install_high()` → `get_install_height()`
- 参数拼写修正：`hight` → `height`
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

## 2026-07-29 — 示例重命名

### 1. 目标跟踪示例
- `examples/readTargetTrajectory/readTargetTrajectory.ino` → `examples/getTargetTracking/getTargetTracking.ino`
- Python：`python/raspberrypi/examples/read_target_trajectory.py` → `get_target_tracking.py`
- 同步更新文件头 `@file` / `@brief` 及相关注释

### 2. 轨迹范围生成示例
- `examples/learningTrajectoryRange/learningTrajectoryRange.ino` → `examples/generateTrajectoryRange/generateTrajectoryRange.ino`
- Python：`learning_trajectory_range.py` → `generate_trajectory_range.py`
- 已删除原目录/原文件，并同步更新 `@file` / `@brief`

## 2026-07-29 — 编译修复

### 1. 类内嵌套类型返回值限定
- 枚举迁入 `DFRobot_C4004` 后，`.cpp` 中成员函数返回类型需加类名限定
- 已修正：`getReportedEvent`、`getPresenceState`、`getMotionState`、`setTag`、`getDetectionRangeMode`、`handlePacket`、`classifyPacket`

## 2026-07-29 — 实际应用示例补充安装高度设置

### 1. 实际应用类示例统一调用安装高度设置
- 在 `begin` 成功后增加 `setInstallHeight(180)` / `set_install_height(180)`（侧装默认 180 cm，推荐 180±20 cm；顶装推荐 220–280 cm）
- 已覆盖：`basicPresenceDetection`、`getTargetTracking`、`generateTrajectoryRange`、`readTagEvent`、`readZoneStateByGPIO`、`setAllParam`，以及 `advancedZoneApplications` 下 `bedStillnessDetection` / `multiZoneStatus` / `roomOccupancyInference`
- Python 对应示例同步补充
- 不改工具类示例：`factoryResetAndReboot` / `factory_reset_and_reboot.py`
- `setAllParam` 打印文案 `install high` → `install height`

## 2026-07-29 — LED 控制接口重命名

### 1. 轨迹 / 占用指示灯接口
- `setTrajectoryLed()` → `setTrkLED()`
- `setMotionLed()` → `setOccLED()`
- 同步 getter：`getTrajectoryLed()` → `getTrkLED()`，`getMotionLed()` → `getOccLED()`
- Python：`set_trajectory_led()` → `set_trk_led()`，`set_motion_led()` → `set_occ_led()`；getter 同步为 `get_trk_led()` / `get_occ_led()`
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

## 2026-07-30 — 占用状态接口重命名

### 1. 占用检测状态获取接口
- `getPresenceState()` → `getOccupancyState()`
- Python：`get_presence_state()` → `get_occupancy_state()`
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

## 2026-07-30 — 回退占用状态接口重命名

### 1. 回退为 Presence 命名
- `getOccupancyState()` → `getPresenceState()`
- Python：`get_occupancy_state()` → `get_presence_state()`
- 相关注释与示例打印（`Presence state:`）一并回退
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

## 2026-07-30 — 人数统计接口重命名

### 1. getPeopleTime → getPeopleCount
- `getPeopleTime()` → `getPeopleCount()`
- Python：`get_people_time()` → `get_people_count()`
- 未改动：`setRealTimePeopleTime()` / `getRealTimePeopleTime()` 及对应 Python 接口
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

## 2026-07-30 — eInstallMode_t 枚举值重命名

### 1. 安装模式枚举值
- `eInstallModeUnknown` → `eUnknown`
- `eInstallModeSide` → `eSide`
- `eInstallModeTop` → `eTop`
- 已同步头文件、实现、Arduino / Python 示例与 README

## 2026-07-30 — 帧生成计数接口重命名

### 1. setCheckToActiveFrames / getCheckToActiveFrames → setFrameGenerateCount / getFrameGenerateCount
- `setCheckToActiveFrames()` → `setFrameGenerateCount()`
- `getCheckToActiveFrames()` → `getFrameGenerateCount()`
- Python：`set_check_to_active_frames()` → `set_frame_generate_count()`，`get_check_to_active_frames()` → `get_frame_generate_count()`
- 注释正文未改
- 已同步头文件、实现、keywords、Arduino / Python 示例与 README

## 2026-08-04 — 人数/轨迹专业名词与 API 重命名

### 1. 公开接口重命名（C++ / Python）
- `set/getRealTimePeopleTime` → `set/getRealTimeReportInterval`（实时上报间隔）
  - Python：`set/get_real_time_people_time` → `set/get_real_time_report_interval`
- `set/getTrackMeters` → `set/getTrajectoryGenerationDistance`（轨迹生成距离）
  - Python：`set/get_track_meters` → `set/get_trajectory_generation_distance`
- `set/getTrackExistsTime` → `set/getTrajectoryLifetime`（轨迹存活时间）
  - Python：`set/get_track_exists_time` → `set/get_trajectory_lifetime`
- `set/getUnmannedTime` → `set/getUnoccupiedTime`（无人占用时间）
  - Python：`set/get_unmanned_time` → `set/get_unoccupied_time`
- `get/clearPeopleCount` → `get/clearLiveCount`（实时人数 Live Count）
  - Python：`get/clear_people_count` → `get/clear_live_count`

### 2. 同步范围
- 已同步头文件、实现、keywords、Arduino / Python 示例打印与 README（中英文）
- 未改：`eTagPeopleCounting`、`eEventPeopleCount` 及内部协议 CMD/CTRL 常量名

## 2026-08-04 — 标签范围形状枚举去掉 TagRange 前缀

### 1. 枚举值重命名
- C++：`eTagRangeCircle` → `eCircle`，`eTagRangeRectangle` → `eRectangle`（类型名 `eTagRangeType_t` 未改）
- Python：`TAG_RANGE_CIRCLE` → `CIRCLE`，`TAG_RANGE_RECTANGLE` → `RECTANGLE`

### 2. 同步范围
- 已同步头文件、实现、keywords、Arduino / Python 相关示例中的用法与注释
